spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/shop_test
    username: shop_test
    password: password
    hikari:
      initialization-fail-timeout: -1
logging:
  level:
    root: debug