spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:${test.database.port}/shop_test
    username: shop_test
    password: password
    hikari:
      initialization-fail-timeout: -1
logging:
  level:
    root: debug